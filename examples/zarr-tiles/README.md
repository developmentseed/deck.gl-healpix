# Healpix Zarr Tiles

Example of using `@developmentseed/deck.gl-healpix-zarr` to render HEALPix tiles from a Zarr store.

## Run the example

After a root `npm install`:

```sh
cd examples/zarr-tiles
cp .env.example .env
npm run dev
```
